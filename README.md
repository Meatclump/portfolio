# Package Tracker

This is a prototype package tracking application.

## Setup instructions

Run the following command to install dependencies:

```bash
npm i
```

Run application with the following command:

```bash
npm run dev
```

Once running, the application will be accessible on localhost and the console will mention which port to use.
